export interface User {
  email: string;
  username: string;
  password?: string;
  // passwordConfirm?: string;
  // firstName: string;
  // lastName: string;
  // phone?: string;
  // resourceId: number;
  // id?: number;
  // createdAt?: Date;
  // role?: string;
  // uid?: string;
  // updatedAt?: Date;
}

export interface Message {
  email: string;
  message: string;
}
