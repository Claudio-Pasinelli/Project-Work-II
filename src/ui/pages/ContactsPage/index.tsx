import { ActionFunctionArgs } from 'react-router-dom';
// import { fullContactFormAction } from '../../../utils/helpers/fullContactFormAction';
import { Contacts } from '../../organisms';

// export async function action(data: ActionFunctionArgs): Promise<ReturnContacts> {
//   return fullContactFormAction(data);
// }

const ContactsPage = () => {
  return <Contacts />;
};

export default ContactsPage;
